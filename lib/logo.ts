// Logo helper retained as a no-op because the project no longer uses a logo image.
export function getLogoPath(): string {
  return ""; // empty string indicates no logo
}

export const LOGO_PATH = "";
